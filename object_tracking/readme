run the object tracking by:
starting the camera:
roslaunch zed_wrapper zed.launch

starting yolo:
roslaunch darknet_ros darknet_ros.launch

run the remaining nodes:
cd catkin_ws
rosrun object_coordinates object_coordinates
rosrun tf_filter tf_filter
rosrun vector_creation vector_creation

max performance mode:
sudo nvpmodel -m 0
sudo ~/jetston_clocks.sh

TODO: 	change it so it only publishes the closest human
	change the yolo config so that it doesnt display an image
	move darknet folder into unicorn map
